services:
  - type: web
    name: test-test-tes-crazy-ammme-ape
    env: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip
      pip3 install --no-cache-dir yt-dlp
      npm install
    startCommand: node server.js
